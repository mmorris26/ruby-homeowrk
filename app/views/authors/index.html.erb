<% @author.each do |author| %>
<h1> <%= link_to "#{author.first_name}  #{author.last_name}", author_path(author)%> </h1>
<h3> <%= link_to "Edit Author", edit_author_path(author)%>
&nbsp;|&nbsp;
 <%= button_to "Delete Author", author_path(author),
            data: {turbo_method: :delete,
                   turbo_confirm: "are you sure??"} %></h3>
<% end %>